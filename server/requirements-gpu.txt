# GPU server job (scripts/build_index.py). Lower bounds are kept loose so an existing
# venv is not force-upgraded. torch must be a CUDA build: run check_env.py for the command.
numpy>=1.24
scipy>=1.10
snowballstemmer>=2.2
transformers>=4.51
sentence-transformers>=3.0
